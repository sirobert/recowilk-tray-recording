namespace MeetingAudioRecorder.Core.Models;

public enum UserExitChoice
{
    SaveRecording,
    PreserveTemporaryFiles,
    Cancel
}

public enum ShutdownAction
{
    ShutdownNow,
    StopAndSave,
    WaitForOperation,
    Cancel
}

