<Window x:Class="MeetingAudioRecorder.App.Views.RecordingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Meeting Audio Recorder — Nagrania i eksport" Height="720" Width="1180"
        MinHeight="560" MinWidth="900" WindowStartupLocation="CenterScreen" Background="#F5F7FA">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <DockPanel Margin="16">
        <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="180"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="Filtr:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox Grid.Column="1" ItemsSource="{Binding Filters}" SelectedItem="{Binding SelectedFilter}"/>
            <TextBox Grid.Column="3" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="Szukaj po tytule lub nazwie pliku" Padding="6"/>
            <Button Grid.Column="4" Content="Odśwież" Command="{Binding RefreshCommand}" Padding="14,6" Margin="8,0,0,0"/>
        </Grid>

        <DockPanel DockPanel.Dock="Bottom" Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="#555"/>
            <ProgressBar DockPanel.Dock="Right" Width="120" Height="12" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>
        </DockPanel>

        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="1.05*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="0.95*"/></Grid.ColumnDefinitions>
            <DataGrid ItemsSource="{Binding Recordings}" SelectedItem="{Binding SelectedRecording}" AutoGenerateColumns="False"
                      IsReadOnly="True" SelectionMode="Single" HeadersVisibility="Column" EnableRowVirtualization="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Data" Binding="{Binding StartedAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="135"/>
                    <DataGridTextColumn Header="Tytuł" Binding="{Binding Title}" Width="*"/>
                    <DataGridTextColumn Header="Czas" Binding="{Binding DurationText}" Width="65"/>
                    <DataGridTextColumn Header="Osoby" Binding="{Binding ParticipantCount}" Width="55"/>
                    <DataGridTextColumn Header="Status eksportu" Binding="{Binding ExportStatusText}" Width="170"/>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="2" Background="White" BorderBrush="#D8DEE8" BorderThickness="1" Padding="16">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel DataContext="{Binding SelectedRecording}">
                        <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding ExportStatusText}" FontSize="15" Foreground="#235E9F" Margin="0,4,0,12"/>
                        <TextBlock Text="Opis" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Description, TargetNullValue=Brak opisu}" TextWrapping="Wrap" Margin="0,2,0,10"/>
                        <TextBlock Text="{Binding StartedAt, StringFormat=Rozpoczęcie: {0:yyyy-MM-dd HH:mm:ss}}"/>
                        <TextBlock Text="{Binding StoppedAt, StringFormat=Zakończenie: {0:yyyy-MM-dd HH:mm:ss}}"/>
                        <TextBlock Text="{Binding ScheduledAt, StringFormat=Termin: {0:yyyy-MM-dd HH:mm}}"/>
                        <TextBlock Text="{Binding Provider, StringFormat=Źródło: {0}}" Margin="0,8,0,0"/>
                        <TextBlock Text="{Binding MeetingUrl, StringFormat=Link: {0}}" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding AudioFileName, StringFormat=Plik: {0}}" Margin="0,8,0,0"/>
                        <TextBlock Text="{Binding SizeText, StringFormat=Rozmiar: {0}}"/>

                        <TextBlock Text="Uczestnicy" FontWeight="SemiBold" Margin="0,14,0,4"/>
                        <DataGrid ItemsSource="{Binding DataContext.SelectedRecording.Participants, RelativeSource={RelativeSource AncestorType=Window}}"
                                  AutoGenerateColumns="False" IsReadOnly="True" MaxHeight="180" HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Nazwa" Binding="{Binding DisplayName}" Width="*"/>
                                <DataGridTextColumn Header="E-mail" Binding="{Binding Email}" Width="*"/>
                                <DataGridTextColumn Header="Rola" Binding="{Binding Role}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <TextBlock Text="Diagnostyka eksportu" FontWeight="SemiBold" Margin="0,14,0,4"/>
                        <TextBlock Text="{Binding ErrorText}" Foreground="#A32626"/>
                        <TextBlock Text="{Binding Attempts, StringFormat=Próby: {0}}"/>
                        <TextBlock Text="{Binding TraceId, StringFormat=TraceId: {0}}" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding MeetingId, StringFormat=MeetingId: {0}}" TextWrapping="Wrap"/>

                        <WrapPanel Margin="0,14,0,0">
                            <Button Content="Ponów eksport" Command="{Binding DataContext.RetryExportCommand, RelativeSource={RelativeSource AncestorType=Window}}" Padding="12,7" Margin="0,0,8,8"/>
                            <Button Content="Otwórz nagranie" Command="{Binding DataContext.OpenRecordingCommand, RelativeSource={RelativeSource AncestorType=Window}}" Padding="12,7" Margin="0,0,8,8"/>
                            <Button Content="Otwórz folder" Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=Window}}" Padding="12,7" Margin="0,0,8,8"/>
                            <Button Content="Kopiuj traceId" Command="{Binding DataContext.CopyTraceIdCommand, RelativeSource={RelativeSource AncestorType=Window}}" Padding="12,7" Margin="0,0,8,8"/>
                        </WrapPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </DockPanel>
</Window>
